# CLI Reference

The standalone command is `cb`. The conda plugin exposes the same parser as
`conda broker`.

```{eval-rst}
.. argparse::
   :module: conda_broker.cli.main
   :func: generate_broker_parser
   :prog: cb
```
